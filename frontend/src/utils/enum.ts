export enum UserSocialActionStatusChoices {
  PENDING = "pending",
  VERIFIED = "verified",
  REQUEST_VERIFY = "request_verify",
}

export enum CodeValidateChoices {
  NONE = "none",
  INPUT_CODE = "input_code",
  VALIDATE_CODE = "validate_code",
}
